image: Ubuntu
stack: node 12, jdk 8, python 2

environment:
  matrix:
    - DRIVER_REPO: "nodejs-driver"
    - DRIVER_REPO: "java-driver"
    - DRIVER_REPO: "csharp-driver"

install:
- source ./install.sh

for:
  - matrix:
      only:
        - DRIVER_REPO: "nodejs-driver"
    test_script:
      # TODO: Use stable $DRIVER_TAG
      - git checkout master
      - export TEST_TRACE=on
      - npm install
      - npm run server_api
  - matrix:
      only:
          - DRIVER_REPO: "java-driver"
    test_script:
      - git checkout $DRIVER_TAG
      - mvn -B -V install -DskipTests
      - mvn -B -V verify --batch-mode --show-version -Dccm.version=$CCM_VERSION -DskipSerialITs -DskipIsolatedITs -Dmaven.javadoc.skip=true
  - matrix:
      only:
        - DRIVER_REPO: "csharp-driver"
    test_script:
      - git checkout $DRIVER_TAG
      - export DOTNET_CLI_TELEMETRY_OPTOUT=1
      - dotnet --version
      - dotnet restore src
      - dotnet test src/Cassandra.IntegrationTests/Cassandra.IntegrationTests.csproj -v n -f netcoreapp2.1 -c Release --filter "(TestCategory=realcluster)"

build: off